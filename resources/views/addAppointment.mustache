<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css" rel="stylesheet">
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.16.0/jquery.validate.min.js"></script>
    <link href="../../../css/page.css" rel="stylesheet">
    <link href="../../../css/style.css" rel="stylesheet">
    <title>Home</title>
</head>
<body>
<div id="mySidebar" class="sidebar">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">×</a>
    <a href="/index">Home</a>
    <a href="/department">Departments</a>
    <a href="/doctor">Doctors</a>
    <a href="/patient">Patients</a>
    <a href="/appointment">Appointments</a>
</div>
<div id="main">
    <button class="openbtn" onclick="openNav()" style="pading-bot:15px">☰ Open Sidebar</button>

    <div class="row">
        <div class="col-75">
            <div class="container">
                <form action="/models/appointment/insert" method="post" class="form" name="doctor" novalidate="novalidate">
                    <div class="row">
                        <div class="col-75">
                            <h3>Add appointment</h3>
                            <label for="note"> Note</label>
                            <input type="text" name="note" placeholder="note">
                            <label for="date" >Appointment Date:</label>
                            <input type="datetime-local" name="date" placeholder="yyyy-mm-dd">
                            <label for="doctor"> Doctor</label>
                            <select name="doctorid">
                                {{#doctor}}
                                    <option value="{{doctorid}}">{{doctorpid}} -- {{name}} {{surname}} </option>
                                {{/doctor}}
                            </select>
                            <label for="patient"> Patient</label>
                            <select name="patientid">
                                {{#patient}}
                                    <option value="{{patientid}}">{{dob}} -- {{name}} {{surname}}</option>
                                {{/patient}}
                            </select>
                        </div>
                        </div>
                    <input type="submit" value="Add a doctor" class="btn-department btn btn-success">
                </form>
            </div>
        </div>
    </div>

</div>
<script src="../../../js/appointment.js"></script>
</body>
</html>